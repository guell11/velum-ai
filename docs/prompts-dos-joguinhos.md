# Prompts dos joguinhos

Todos os prompts abaixo foram usados exatamente como estão nos exemplos locais do dataset.

## Flappy Bird

Crie um jogo Flappy Bird completo e responsivo em um único arquivo HTML. Retorne somente o HTML completo.

## Jogos Three.js principais

### `bonsai.html`

Crie uma experiência 3D interativa de um bonsai voxel em um único HTML. Retorne somente o HTML completo.

### `cyber_highway_1984_ultimate.html`

Crie um jogo 3D de carro em uma rodovia cyberpunk retrô em um único HTML. Retorne somente o HTML completo.

### `doom_threejs.html`

Crie um jogo de tiro 3D inspirado em Doom usando Three.js em um único HTML. Retorne somente o HTML completo.

### `game_01_railway_rush.html`

Crie um joguinho 3D de carro em uma corrida ao lado de uma ferrovia. Retorne somente o HTML completo.

### `game_02_desert_rally.html`

Crie um joguinho 3D de rali de carro no deserto. Retorne somente o HTML completo.

### `game_03_neon_night.html`

Crie um joguinho 3D de corrida de carro em um circuito neon noturno. Retorne somente o HTML completo.

### `game_04_alpine_pass.html`

Crie um joguinho 3D de carro atravessando uma estrada alpina. Retorne somente o HTML completo.

### `game_05_forest_courier.html`

Crie um joguinho 3D de entregas de carro em uma floresta. Retorne somente o HTML completo.

### `game_06_harbor_sprint.html`

Crie um joguinho 3D de corrida de carro em um porto. Retorne somente o HTML completo.

### `game_07_volcano_run.html`

Crie um joguinho 3D de carrinho correndo em um vulcão. Retorne somente o HTML completo.

### `game_08_beach_buggy.html`

Crie um joguinho 3D de buggy correndo na praia. Retorne somente o HTML completo.

### `game_09_canyon_drift.html`

Crie um joguinho 3D de drift de carro em um cânion. Retorne somente o HTML completo.

### `game_10_farm_dash.html`

Crie um joguinho 3D de corrida de carro em uma fazenda. Retorne somente o HTML completo.

### `game_11_airport_ground_rush.html`

Crie um joguinho 3D de carro correndo pelas pistas de um aeroporto. Retorne somente o HTML completo.

### `game_12_junkyard_escape.html`

Crie um joguinho 3D de carro escapando de um ferro-velho. Retorne somente o HTML completo.

### `game_13_moon_rover_race.html`

Crie um joguinho 3D de corrida de rover na Lua. Retorne somente o HTML completo.

### `game_14_rainy_city_taxi.html`

Crie um joguinho 3D de táxi em uma cidade chuvosa. Retorne somente o HTML completo.

### `game_15_ice_lake_challenge.html`

Crie um joguinho 3D de carro correndo sobre um lago congelado. Retorne somente o HTML completo.

### `game_16_autumn_hill_climb.html`

Crie um joguinho 3D de subida de carro em colinas de outono. Retorne somente o HTML completo.

### `game_17_construction_runner.html`

Crie um joguinho 3D de carro desviando de obstáculos em uma obra. Retorne somente o HTML completo.

### `game_18_sunset_highway.html`

Crie um joguinho 3D de perseguição de carro em uma rodovia ao pôr do sol. Retorne somente o HTML completo.

### `game_19_toy_room_racers.html`

Crie um joguinho 3D de carrinhos de brinquedo correndo em um quarto. Retorne somente o HTML completo.

### `game_20_coastal_train_chase.html`

Crie um joguinho 3D de carro perseguindo um trem no litoral. Retorne somente o HTML completo.

### `gemini-code-1787424277975.html`

Crie uma experiência 3D futurista com um núcleo quântico interativo em um único HTML. Retorne somente o HTML completo.

### `sistema solar.html`

Crie um sistema solar 3D interativo em um único HTML. Retorne somente o HTML completo.

### `terra.html`

Crie uma visualização 3D interativa do planeta Terra em um único HTML. Retorne somente o HTML completo.

## Jogos incluídos uma vez

### `qualidadeabaixo colocar 1x no dataset no maximo/air hockey.html`

Crie um jogo de air hockey neon em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/basquete horsey.html`

Crie um jogo de basquete HORSE em um terraço em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/batalha de desenho.html`

Crie um jogo de batalha de desenhos em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/batalha de pioes.html`

Crie um jogo de batalha de piões em uma arena em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/batalha naval 3d ativa.html`

Crie um jogo de batalha naval 3D ativa em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/clash royale.html`

Crie um jogo de estratégia de arena com torres e tropas em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/corrida de obstaculos procedural.html`

Crie uma corrida procedural de obstáculos em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/duelo de arqueiros.html`

Crie um jogo de duelo de arqueiros em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/duelo de feiticos.html`

Crie um jogo de duelo de feitiços em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/duelo de gravidade.html`

Crie um jogo de duelo com gravidade orbital em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/duelo de luz tron.html`

Crie um jogo de duelo de motos de luz em uma grade neon em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/duelo de penaltis.html`

Crie um jogo de duelo de pênaltis em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/duelo de tanques.html`

Crie um jogo de duelo de tanques com tiros ricocheteando em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/guerra de escalada.html`

Crie um jogo competitivo de escalada em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/luta de espadas low poly.html`

Crie um jogo de luta de espadas low poly em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/minigolfe competitivo.html`

Crie um jogo de minigolfe competitivo em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/pong 3d.html`

Crie um jogo de Pong 3D em uma fortaleza no céu em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/queda de blocos spleef.html`

Crie um jogo de spleef com blocos caindo em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/sumo de esferas.html`

Crie um jogo de sumô de esferas em um único HTML. Retorne somente o HTML completo.

### `qualidadeabaixo colocar 1x no dataset no maximo/tenis de mesa primeira pessoa.html`

Crie um jogo de tênis de mesa em primeira pessoa em um único HTML. Retorne somente o HTML completo.

## Variações multilíngues

### `flappybird.html`

- **Português:** Faça um jogo completo de Flappy Bird em um único arquivo HTML. Retorne apenas o HTML.

- **English:** Create a complete Flappy Bird game in a single HTML file. Return only the HTML.

- **Español:** Crea un juego completo de Flappy Bird en un único archivo HTML. Devuelve solo el HTML.

- **Français:** Créez un jeu Flappy Bird complet dans un seul fichier HTML. Retournez uniquement le HTML.

- **Deutsch:** Erstelle ein vollständiges Flappy-Bird-Spiel in einer einzigen HTML-Datei. Gib nur das HTML zurück.

- **Italiano:** Crea un gioco completo di Flappy Bird in un unico file HTML. Restituisci solo l'HTML.

- **日本語:** Flappy Birdの完全なゲームを1つのHTMLファイルで作成してください。HTMLだけを返してください。

- **中文:** 请在单个 HTML 文件中创建一个完整的 Flappy Bird 游戏。只返回 HTML。

- **Русский:** Создай полноценную игру Flappy Bird в одном HTML-файле. Верни только HTML.

- **Türkçe:** Tek bir HTML dosyasında eksiksiz bir Flappy Bird oyunu oluştur. Yalnızca HTML döndür.

### `aspirador_de_notificacoes.html`

- **Português:** Crie um jogo 3D chamado Aspirador de Notificações em um único HTML. Retorne apenas o HTML.

- **English:** Create a 3D game called Notification Vacuum in a single HTML file. Return only the HTML.

- **Español:** Crea un juego 3D llamado Aspiradora de Notificaciones en un único HTML. Devuelve solo el HTML.

- **Français:** Créez un jeu 3D appelé Aspirateur de notifications dans un seul fichier HTML. Retournez uniquement le HTML.

- **Deutsch:** Erstelle ein 3D-Spiel namens Benachrichtigungs-Staubsauger in einer einzigen HTML-Datei. Gib nur das HTML zurück.

- **Italiano:** Crea un gioco 3D chiamato Aspirapolvere delle notifiche in un unico file HTML. Restituisci solo l'HTML.

- **日本語:** 通知を吸い込む3Dゲームを1つのHTMLファイルで作成してください。HTMLだけを返してください。

- **中文:** 请在单个 HTML 文件中创建一个吸走通知的 3D 游戏。只返回 HTML。

- **Русский:** Создай 3D-игру «Пылесос уведомлений» в одном HTML-файле. Верни только HTML.

- **Türkçe:** Bildirim Süpürgesi adlı 3D oyunu tek bir HTML dosyasında oluştur. Yalnızca HTML döndür.

### `bORNAUT.html`

- **Português:** Crie um jogo 3D chamado Fuja do Burnout em um único HTML. Retorne apenas o HTML.

- **English:** Create a 3D game called Escape Burnout in a single HTML file. Return only the HTML.

- **Español:** Crea un juego 3D llamado Escapa del Burnout en un único HTML. Devuelve solo el HTML.

- **Français:** Créez un jeu 3D appelé Échappez au burnout dans un seul fichier HTML. Retournez uniquement le HTML.

- **Deutsch:** Erstelle ein 3D-Spiel namens Entkomme dem Burnout in einer einzigen HTML-Datei. Gib nur das HTML zurück.

- **Italiano:** Crea un gioco 3D chiamato Fuggi dal burnout in un unico file HTML. Restituisci solo l'HTML.

- **日本語:** 燃え尽き症候群から逃げる3Dゲームを1つのHTMLファイルで作成してください。HTMLだけを返してください。

- **中文:** 请在单个 HTML 文件中创建一个逃离职业倦怠的 3D 游戏。只返回 HTML。

- **Русский:** Создай 3D-игру «Побег от выгорания» в одном HTML-файле. Верни только HTML.

- **Türkçe:** Tükenmişlikten Kaçış adlı 3D oyunu tek bir HTML dosyasında oluştur. Yalnızca HTML döndür.

### `defenda_o_servidor.html`

- **Português:** Crie um jogo 3D de defender um servidor em um único HTML. Retorne apenas o HTML.

- **English:** Create a 3D server defense game in a single HTML file. Return only the HTML.

- **Español:** Crea un juego 3D de defensa de servidores en un único HTML. Devuelve solo el HTML.

- **Français:** Créez un jeu 3D de défense de serveur dans un seul fichier HTML. Retournez uniquement le HTML.

- **Deutsch:** Erstelle ein 3D-Spiel zur Verteidigung eines Servers in einer einzigen HTML-Datei. Gib nur das HTML zurück.

- **Italiano:** Crea un gioco 3D di difesa del server in un unico file HTML. Restituisci solo l'HTML.

- **日本語:** サーバーを守る3Dゲームを1つのHTMLファイルで作成してください。HTMLだけを返してください。

- **中文:** 请在单个 HTML 文件中创建一个保卫服务器的 3D 游戏。只返回 HTML。

- **Русский:** Создай 3D-игру о защите сервера в одном HTML-файле. Верни только HTML.

- **Türkçe:** Sunucu savunma temalı 3D oyunu tek bir HTML dosyasında oluştur. Yalnızca HTML döndür.

### `deploy_na_sexta_18h.html`

- **Português:** Crie um jogo 3D chamado Deploy na Sexta às 18h em um único HTML. Retorne apenas o HTML.

- **English:** Create a 3D game called Friday 6 PM Deploy in a single HTML file. Return only the HTML.

- **Español:** Crea un juego 3D llamado Deploy del viernes a las 18:00 en un único HTML. Devuelve solo el HTML.

- **Français:** Créez un jeu 3D appelé Déploiement du vendredi à 18 h dans un seul fichier HTML. Retournez uniquement le HTML.

- **Deutsch:** Erstelle ein 3D-Spiel namens Freitags-Deployment um 18 Uhr in einer einzigen HTML-Datei. Gib nur das HTML zurück.

- **Italiano:** Crea un gioco 3D chiamato Deploy del venerdì alle 18 in un unico file HTML. Restituisci solo l'HTML.

- **日本語:** 金曜日18時のデプロイをテーマにした3Dゲームを1つのHTMLファイルで作成してください。HTMLだけを返してください。

- **中文:** 请在单个 HTML 文件中创建一个“周五下午六点部署”的 3D 游戏。只返回 HTML。

- **Русский:** Создай 3D-игру «Деплой в пятницу в 18:00» в одном HTML-файле. Верни только HTML.

- **Türkçe:** Cuma Saat 18.00 Deploy adlı 3D oyunu tek bir HTML dosyasında oluştur. Yalnızca HTML döndür.

### `luz_no_fim_do_tunel.html`

- **Português:** Crie um jogo 3D chamado Luz no Fim do Túnel em um único HTML. Retorne apenas o HTML.

- **English:** Create a 3D game called Light at the End of the Tunnel in a single HTML file. Return only the HTML.

- **Español:** Crea un juego 3D llamado Luz al Final del Túnel en un único HTML. Devuelve solo el HTML.

- **Français:** Créez un jeu 3D appelé La lumière au bout du tunnel dans un seul fichier HTML. Retournez uniquement le HTML.

- **Deutsch:** Erstelle ein 3D-Spiel namens Licht am Ende des Tunnels in einer einzigen HTML-Datei. Gib nur das HTML zurück.

- **Italiano:** Crea un gioco 3D chiamato Luce in fondo al tunnel in un unico file HTML. Restituisci solo l'HTML.

- **日本語:** トンネルの先の光をテーマにした3Dゲームを1つのHTMLファイルで作成してください。HTMLだけを返してください。

- **中文:** 请在单个 HTML 文件中创建一个“隧道尽头的光”3D 游戏。只返回 HTML。

- **Русский:** Создай 3D-игру «Свет в конце тоннеля» в одном HTML-файле. Верни только HTML.

- **Türkçe:** Tünelin Sonundaki Işık adlı 3D oyunu tek bir HTML dosyasında oluştur. Yalnızca HTML döndür.

### `semaforo_do_caos.html`

- **Português:** Crie um jogo 3D chamado Semáforo do Caos em um único HTML. Retorne apenas o HTML.

- **English:** Create a 3D game called Chaos Traffic Light in a single HTML file. Return only the HTML.

- **Español:** Crea un juego 3D llamado Semáforo del Caos en un único HTML. Devuelve solo el HTML.

- **Français:** Créez un jeu 3D appelé Feu tricolore du chaos dans un seul fichier HTML. Retournez uniquement le HTML.

- **Deutsch:** Erstelle ein 3D-Spiel namens Chaos-Ampel in einer einzigen HTML-Datei. Gib nur das HTML zurück.

- **Italiano:** Crea un gioco 3D chiamato Semaforo del caos in un unico file HTML. Restituisci solo l'HTML.

- **日本語:** カオスな信号機をテーマにした3Dゲームを1つのHTMLファイルで作成してください。HTMLだけを返してください。

- **中文:** 请在单个 HTML 文件中创建一个“混乱红绿灯”3D 游戏。只返回 HTML。

- **Русский:** Создай 3D-игру «Светофор хаоса» в одном HTML-файле. Верни только HTML.

- **Türkçe:** Kaos Trafik Işığı adlı 3D oyunu tek bir HTML dosyasında oluştur. Yalnızca HTML döndür.
